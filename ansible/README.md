# ansible/

Ansible playbooks for configuring nodes (VMs, bare-metal) after provisioning.

**Status:** Placeholder — not yet implemented.

Planned:
- `playbooks/k3s-install.yml` — Install K3s on target nodes
- `playbooks/gpu-drivers.yml` — Install NVIDIA drivers for GPU nodes
- `roles/` — Reusable roles (docker, k3s, nvidia)
